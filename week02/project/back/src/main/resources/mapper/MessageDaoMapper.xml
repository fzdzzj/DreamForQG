<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.qg.dormrepair.mapper.MessageDao">

    <!-- 查询用户消息列表（分页 + 支持类型筛选 + 阅读状态筛选） -->
    <select id="selectByAccountWithPage" resultType="com.qg.dormrepair.pojo.Message">
        SELECT id, user_account, title, content, type, is_read, related_id, create_time
        FROM message
        WHERE user_account = #{userAccount}

        <!-- 阅读状态筛选 -->
        <if test="isRead != null">
            AND is_read = #{isRead}
        </if>
        <if test="type != null and type != ''">
            AND type = #{type}
        </if>

        ORDER BY is_read ASC, create_time DESC
        LIMIT #{offset}, #{pageSize}
    </select>

    <!-- 批量删除消息 -->
    <delete id="deleteBatch">
        DELETE FROM message
        WHERE id IN
        <foreach item="id" collection="messageIds" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND user_account = #{userAccount}
    </delete>

    <!-- 批量标记为已读 -->
    <update id="markBatchAsRead">
        UPDATE message SET is_read = '1'
        WHERE id IN
        <foreach item="id" collection="messageIds" open="(" separator="," close=")">
            #{id}
        </foreach>
        AND user_account = #{userAccount}
    </update>

</mapper>